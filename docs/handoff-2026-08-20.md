# Handoff — 2026-08-20

**State:** Phases 1–4 passed. Phase 5 (build) started — `docs/build-plan.md` written, its §1
ground checklist fully closed. No product code exists yet.

**Live:**
- Neon project `withered-violet-63924132`, migration applied, 5 domain tables confirmed
- GitHub: `modryn-studio/plot`, pushed to `main`
- Vercel: `https://plot-steel.vercel.app`, env vars set including `NEXT_PUBLIC_SITE_URL` /
  `BETTER_AUTH_URL`, redeployed

**Read first:** `CLAUDE.md`, then `docs/build-plan.md` — that's the slice list and wave table.

**Next:** slice 0, `ground` — the walking skeleton. `build-plan.md` §3 has the notes, §1 has
nothing left blocking it.

**Still open:** five decisions in `spec.md` §6 (generation model, render faithfulness, plant
palette source, guide count, guide grounding) — none block slice 0/1, `build-plan.md` §4 names
which wave each blocks.
